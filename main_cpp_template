
#include <vector>
#include <iostream>
using namespace std;


int main(int argc, char const *argv[])
{
    vector<bool> tests {
        
    };
    bool result = true;
    for (bool b : tests) result = result && b;
    result ? (cout << "fail\n") : (cout << "good\n");
    return 0;
}